<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- authentication_store.json contains access tokens — excluded for security -->
    <exclude
        domain="file"
        path="authentication_store.json" />

    <!-- UserPreferences -->
    <include
        domain="sharedpref"
        path="org.jellyfin.androidtv_preferences" />
    <include
        domain="sharedpref"
        path="org.jellyfin.androidtv.debug_preferences" />
    <!-- SystemPreferences -->
    <include
        domain="sharedpref"
        path="systemprefs" />
    <!-- AuthenticationPreferences -->
    <include
        domain="sharedpref"
        path="authentication" />

    <!-- Jellyseerr prefs contain API keys and passwords — excluded for security.
         Per-user jellyseerr_prefs_$userId files are also excluded: the include rules
         above form an allowlist, and backup paths do not support wildcards. -->
    <exclude
        domain="sharedpref"
        path="jellyseerr_prefs" />
</full-backup-content>
